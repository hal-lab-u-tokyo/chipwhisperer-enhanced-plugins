version: '3.8'

services:
  notebook:
    build:
      context: .
      dockerfile: ./docker/Dockerfile-intel-gpu
    image: cw-ext-notebooks:intel-gpu

    security_opt:
      - seccomp:unconfined
    group_add:
      - video
    devices:
      - /dev/dri:/dev/dri
      - /dev/kfd:/dev/kfd

